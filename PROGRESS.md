# DOMORA — Progress

> Status board. Solo project — no assignee column. Update when starting or
> finishing a component; the Activity Log is append-only.

## Legend

📋 Todo · 🔄 Ongoing · ✅ Done · 🚫 Blocked

## Software (hub runtime)

| Component | Status | Notes |
| --- | --- | --- |
| Engineering spec + critical review | ✅ Done | docs/DOMORA_SPEC.md — 25 sections, MVP defined in §25.6 |
| Event bus + twin state + knowledge graph | ✅ Done | hub/core, hub/twin — stdlib only |
| Agent loop (observe→…→verify) | ✅ Done | 7 agents, expectation contract, capability-table safety |
| Virtual house simulator + fault injection | ✅ Done | sim/ — leak + stuck-valve scenarios |
| Closed-loop tests | ✅ Done | leak + stuck-valve scenarios (`python -m pytest tests/ -q`) |
| Pump dry-run protection loop (2nd MVP loop) | ✅ Done | sim/virtual_pump.py (tank+pump, dry-run + welded-relay fault injection) · PumpProtection virtual sensor (pump running current + flat tank level → dry-run suspected) · planner `_rule_dryrun_response` cuts pump, verified via CT clamp independent of relay ACK. `--scenario dryrun` / `dryrun_stuck`. No capability-table change (`pump:off` already listed; `pump:on` already unrepresentable = the spec's FAULT-state guard for free). 3 tests: cut verified, welded relay escalates + marks pump suspect, healthy pump never trips. **Second of the two §25.6 closed loops now closed** |
| Panel sensor-chain fidelity model (sim/virtual_loads.CtChain) | ✅ Done | Models what nodes/panel_node publishes on the CT-derived path (`I × V × assumed_PF`) so the cost of that config is **measured, not claimed**: a CT clamp cannot measure the power factor it divides out, so every load comes out scaled by `assumed_PF/true_PF`. Real numbers through the real NILM primitives — fridge (motor, PF 0.62) **+53.3%**, kettle (resistive, PF 1.00) −5.0%, lamp (PF 0.95) −0.7%. Clustering survives (3 distinct, correctly-ordered clusters, so the demo still works); the energy ledger does not — which is why `POWER_FROM_PZEM = 1` is the recommended config. Default simulator behaviour byte-identical (opt-in, `ct_chain=None`), so the existing NILM tests are untouched. 2 tests. Observer gained voltage_v/frequency_hz/power_factor/energy_kwh ranges + 5 tests, incl. one asserting a genuine surge/brownout is **recorded rather than quarantined** (a range drawn tightly around 230 V would discard exactly the events spec §11.1 wants logged) |
| Comfort loop — IR-commanded AC (3rd closed loop) | ✅ Done | **Scope note: not in PLAN.md's 8 weeks or spec §25.6** — added on owner request after a discussion of how DOMORA reaches appliances of other brands that aren't IoT at all. sim/virtual_comfort.py (first-order RC room + compressor with spin-up lag + `ir_blind` fault injection) · `ACRunning` virtual sensor (hub/twin/virtual_sensors.py) · planner `_rule_comfort_response` · scenarios `comfort` / `comfort_blind`. **Why it earns its place architecturally:** every actuator until now was one the hub could also sense (valve position, pump CT). An IR-driven AC answers *nothing* — there is no ACK even in principle — so the expectation contract carries the entire weight of knowing whether a command landed. Verified via a step change at the panel CT, a sensor the appliance cannot influence. The `comfort_blind` scenario is the point: the blaster fires, `living_ac.ir_last_cmd` reads "on", and a command-trusting system would report success — while the panel says no compressor ever started, verification fails after one retry, and `health.living_ac` goes suspect. Deliberately no protocol decoding: raw captured mark/space replay works on appliances no brand table covers. **Capability-table change with stated invariant review** (hub/agents/safety.py): `living_ac:on` is the first capability that *energizes* rather than stops something, bounded by a new invariant — never run the AC in an empty house. Review names the gap it does *not* close: the table admits `on`/`off` but not a mode, so a reversible heat pump learned from its HEAT button would pass every hub check; enforcement lives in which IR code gets captured. 7 tests, 73 total green |
| Benchmark harness (tools/benchmark.py) | ✅ Done | Full pipeline + SQLite under synthetic node load; verdict thresholds baked in. Dev-PC dry run: 24.5k msg/s = 245× headroom over a 10-node house |
| UNO Q benchmark run (week-1 gate) | ✅ Done | **Run on the real board** (adb shell, aarch64 Debian 13, Python 3.13.5, 3.6 GiB RAM, 4 cores). `python3 -m tools.benchmark --nodes 10 --points 5 --rate 2 --duration 60`: **30.3x headroom, COMFORTABLE** (≥10x threshold, spec §25.3), 330 µs/msg pipeline latency, 26.8 KB peak Python heap. 3 runs, consistent (29.7–30.4x). Stress test at 2x the house (20 nodes, 8 points, 5 Hz, 120s) still holds 3.6x ("acceptable"), showing the real ceiling is well past MVP scale. **Runs natively on the UNO Q — no LAN-sidecar fallback needed.** Also ran all 19 stdlib-only tests directly on the board (no pytest installed there; wrote a tiny dependency-free runner) — every closed-loop, dashboard, NILM, and journaling test passes on the real target architecture, not just x86 dev machine |
| MQTT bridge (real broker) | ✅ Done | hub/services/mqtt_bridge.py — role-filtered hub/node directions (loop-proof), queue-pumped into the single-threaded twin. E2E test spawns a private Mosquitto: leak loop closes across the wire. **Also verified against a real, apt-installed Mosquitto deployed on the UNO Q itself**, hub running on the board, a simulated field node reaching it over the actual home Wi-Fi from a separate machine — leak detected, planned, dispatched, valve closed, verified, all over the real network |
| MQTT mTLS + per-node certs | ✅ Done | tools/gen_certs.py provisions the house PKI (CA + broker + per-identity client certs); cert CN = broker username, ACL confines each node to its own namespace. 4 tests (was 3): TLS roundtrip works, cert-less client rejected, foreign-namespace publish dropped, **broker cert SAN covers real LAN hosts** (`--broker-host`). Found and fixed a real bug deploying the broker on the UNO Q: the broker cert only ever validated for localhost/127.0.0.1, so any real network client (a genuine ESP32, or a client reached over Wi-Fi rather than loopback) failed TLS hostname verification outright — the exact case every real node needs to work |
| SQLite journaling (twin + action log) | ✅ Done | hub/services/store.py — WAL mode, journals every twin change + full action audit trail (cause/evidence/expectation/outcome); 2 tests; `--journal db` flag on hub.main |
| NILM event-clustering | ✅ Done | hub/twin/nilm.py + EnergySense agent: edge detection between stable levels, incremental signature clustering, on/off pairing → per-appliance energy ledger, interactive labelling (label_nearest). `--scenario energy` recovers fridge/kettle/lamp from the aggregate alone (kettle 7.49 Wh vs 7.5 true). 4 tests |
| Dashboard — live WS transport + operations view | ✅ Done | hub/services/api.py — stdlib-only HTTP + RFC 6455 WebSocket (no FastAPI, runs dependency-free on the UNO Q). Taps the live bus + twin, broadcasts every event/point, replays history to late joiners, per-client queue so a slow phone can't stall the hub. dashboard/index.html renders the cause→evidence→command→verify timeline + live points table, theme-aware. 2 tests: a real WS client watches the leak loop close and the stuck valve escalate. `python -m hub.services.api --scenario stuck` |
| Dashboard — journal playback (scrub-back) | ✅ Done | hub/services/store.py `JournalReader` rebuilds a recorded run as the same frame contract the live WS uses; `/playback.json` serves it; dashboard scrubber replays through the identical renderers. `python -m hub.main --scenario stuck --journal demo.db` then `python -m hub.services.api --playback demo.db`. 2 tests: timeline reconstruction + endpoint |
| DOMORA Mobile (Flutter) — Phases C+D+E (dashboards, detail pages, history/insights/settings/search) | ✅ Done | docs/APP_PLAN.md §9 — **all 5 mobile phases now complete; the mobile app reaches full parity with the web Studio.** Phase C: `widgets/line_chart.dart` (CustomPainter — line + 10%-opacity area, hairline grid, end label, touch-driven crosshair readout since a phone has no hover) and `widgets/bar_chart.dart` (widget-composed rather than one painted canvas, so each bar is independently tappable and screen-reader-legible), both to the same `dataviz` mark specs as `studio/ui/*.js`, reusing the palette validated during the Studio build rather than re-deriving it; Energy/Water/Security screens. Phase D: Room/Appliance/Sensor, reached by click-through (Home zone → Room → Sensor, Energy bar → Appliance) exactly as in Studio — `pushScoped()` added to `core/app_scope.dart` because a pushed route is a *sibling* of AppShell's route, not a descendant, so it would not otherwise inherit StoreScope/HubScope. Phase E: `core/playback.dart` (timeline reconstruction lifted into core/ so time-awareness is unit-testable, not only reachable through a widget), History scrubber, `core/insights.dart`, Settings, and a search screen — **navigate-only, no actuation**, same reasoning as Studio's ⌘K bar (safety.py's capability table is the sole actuation path and CLAUDE.md requires an invariant review to change it), with a test that asserts every search result is a destination. Deleted the now-unused `coming_soon.dart` placeholder. **Every fixture is real captured server output** (`/nilm`, `/graph`, `/history`, `/playback.json` curled from live hubs this session, replayed as raw bytes), never hand-written. 34 new tests → 47 total, `flutter analyze` clean. **Three real bugs found by exercising, not inspecting:** (1) `StatTile` overflowed horizontally in a 3-up grid on a real 420px surface (a value like `1.86 kW` at 22pt) — fixed with a scale-down `FittedBox`, which protects every caller; (2) the same tiles overflowed *vertically* by ~1px at the Energy/Appliance grid aspect ratios — fixed; (3) History's and Room's point rows overflowed by 456px on map-valued twin points (`virtual.water.leak_evidence` renders as a wide string) — both columns now bounded and ellipsized. A fourth failure turned out to be the test harness, not the app: `InkWell` needs a `Material` ancestor, which `AppShell`'s `Scaffold` supplies in the real app — the shared harness now mirrors the app instead of the app being changed to suit the harness. **Verified live, three ways:** the app's real data layer driven against two real running hubs over a real WebSocket and real HTTP (real `/nilm` kettle 7.495 Wh, real `/graph` 11 assets, `connection=live t=100 16 points`, real `/playback.json` 1381 frames scrubbing t=0 → 0 actions vs t=119 → 1 failed action + leak, real `/history` 120 points); the whole app compiled for a real target; and the compiled app screenshotted in a real browser against a live `stuck` hub **mid-incident at t=043, rendering the real cognitive chain** (`leak:main_line → line flow falls below 0.1 L/min · pending`, valve still green) — and again at t=112/119 with the valve red after the real t=56 escalation, i.e. the live state genuinely evolving. Windows desktop (a native target, which would have dodged the web target's CORS limit) was attempted first but needs Developer Mode enabled — a system setting left to the owner |
| DOMORA Mobile (Flutter) — Phase B (DOMORA AI chat) | ✅ Done | mobile/lib/screens/ai.dart — direct port of studio/screens/ai.js: chat transcript, quick-prompt chips, evidence cards, disabled mic stub. Refactored `HubClient` to accept an injectable `http.Client` (defaults to a real one; tests substitute `package:http/testing.dart`'s `MockClient`) — the standard Flutter pattern for testing network code without a live server. Tests use **byte-accurate real captured `/ai` response bodies** (curled from the live hub this session), not invented JSON. Found and fixed a real fixture bug in the process: hand-retyping the curled JSON replaced the server's actual escaped `—` (Python's `json.dumps(ensure_ascii=True)` — the real wire bytes are ASCII, never raw UTF-8) with a literal em-dash character, which isn't Latin-1-encodable and made `http.Response(...)` throw — masked as an unrelated widget-test failure until traced back. 7 new tests (hub_client_test.dart, ai_screen_test.dart), 13 total in the mobile suite, all green, plus `flutter analyze` clean. **Verified the CORS limitation empirically rather than assuming it**: `curl` with a cross-origin `Origin` header against the real `/ai` endpoint confirmed no `Access-Control-Allow-Origin` response header, meaning a real browser would block the Flutter *web* target from reading the response — this is why HTTP-based screens are verified via `MockClient`-fed real data rather than a live Chrome session (which would just hit the expected fallback); native targets (the actual ship target) have no such restriction |
| DOMORA Mobile (Flutter) — Phase A (foundation + Home) | ✅ Done | docs/APP_PLAN.md §9. Owner asked for full Studio parity in Flutter, not a thinner companion. New `mobile/` Flutter project (Flutter 3.44.5 / Dart 3.12.2, already installed) — 3 deps only (web_socket_channel, http, shared_preferences), no state-management or charting package: `ChangeNotifier` + planned `CustomPainter` charts. Direct Dart ports of the already-proven web contract: `core/store.dart` (store.js), `core/hub_client.dart` (socket.js's reconnect logic + the HTTP endpoints), `core/twin.dart` (twin.js's derive* functions, including the already-fixed "empty house = Learning" bug — ported correct, not re-broken). Connect screen (host:port, since unlike Studio this app is a separate process from the hub and has to be told where it lives — persisted via shared_preferences) + Home screen (hero zones, vitals rail, thinking strip). `flutter analyze`: clean. `flutter test`: 6/6 green, fed the **same real captured `/ws` frames** already used to verify Studio's web build (not new hand-written fixtures) — caught and fixed a real layout bug this way (hero header `Row` overflowed on a 400px-wide test surface; fixed with `Expanded`+`Wrap`). **Verified live, twice over**: (1) the connected Android target turned out to be missing `getprop` even under raw `adb shell` (confirmed independent of Flutter tooling) — a sandboxed/minimal Android instance, not a fully functional device, so undeployable from here; (2) fell back to the Chrome web target — the real compiled app, in a real browser, over a real WebSocket, tracked the actual running hub's leak escalation live end-to-end (`leak=false`→`leak=true, valveHealth=suspect`, ticks advancing) via a temporary debug entrypoint (deleted after verification) that bypassed only the Connect screen's HTTP health-check, which browser CORS blocks cross-origin for the web target specifically — a property of that target, not the app; native targets (the actual ship target) have no such restriction. **Honest scope**: AI/Energy/Water/Security tabs are explicit "Phase B/C — not built yet" placeholders, same discipline as Studio's pre-launch "· soon" nav chips |
| DOMORA Mobile — open without a gate (owner request, 2026-08-04) | ✅ Done | The Connect screen built in Phase A above worked but had a real UX cost: no saved/reachable hub address meant the app was completely unusable, screen and all — you couldn't even look around. Owner asked for the app to open regardless, with the address entered afterward. `HubClient.baseUrl` is now mutable; `connect()` guards an empty address into a new `unconfigured` `DomoraStore.connection` state instead of lying with `connecting`, and a new `reconnectTo(url)` cancels any in-flight attempt, resets the store (no stale points from a *different* hub), and redials — called from a new **Settings → Hub connection** card (address field, an inline non-blocking reachability hint via the existing `healthCheck()`, and the `10.0.2.2:8080` emulator shortcut carried over from the old screen). `main.dart` now builds `AppShell` directly; a saved address (if any) loads and dials in the background. `connect.dart` deleted outright rather than left as a dead file. AppShell's status pill gained the `unconfigured` state and is now tappable, and a new dismiss-free banner across every tab appears whenever the hub isn't live, both routing to the same Settings card — the discoverability the old gate gave for free, without the lockout. 9 new tests (5 in hub_client_test.dart proving the empty-address/reconnect/persistence paths never touch a real socket, 4 in phase_e_test.dart for the new card), 56 total green, `flutter analyze` clean. **Verified live on a real device, not just widget tests**: built and installed the debug APK on a genuinely-connected Android 14 phone (`adb`/`getprop`/`screencap` all functional this session, unlike Phase A's sandboxed target) reachable over real home Wi-Fi; ran a real hub (`--scenario stuck`) on this machine's LAN IP. Screenshots at each step confirm the actual claim: (1) app data cleared to simulate a genuine first-ever launch → opens straight to Home with an honest "not set up" pill and banner, zero gate; (2) tapping the banner reaches Settings; (3) typing the real LAN address and tapping "Save & connect" flips the pill green and streams the real leak-suspected/critical escalation from the live hub; (4) force-stopping and relaunching the app reconnects to the saved address automatically, live data flowing again, no re-prompt. Port 8080 turned out to already be bound by an unrelated process on this machine — found via `netstat`, worked around with `--port 8081` rather than fighting it |
| DOMORA Studio — Phase 5 (Command bar, History, AI Insights, Settings) | ✅ Done | studio/ui/command-bar.js (⌘K/Ctrl+K global overlay, search-and-navigate over screens/rooms/live points) — **deliberately no "issue a command" feature**: this system's only actuation path is the planner through hub/agents/safety.py's capability table, and CLAUDE.md requires an explicit invariant review for capability-table changes, so a manual-command surface isn't something to add via a search box without that review. studio/core/insights.js + screens/insights.js: prioritized observation cards (critical/warning/info) derived from real signals already shown elsewhere (leak, dry-run, health-suspect, low tank, escalated/retrying actions, top NILM consumer) — a different presentation of real state, not a new inference engine. studio/screens/history.js: scrubs a real recorded journal via GET /playback.json, reconstructing twin+action state **at the scrubbed tick** (time-aware, not just replaying everything) — kept as its own local state, not the live store, so scrubbing the past can't corrupt what other tabs show as "now"; honest fallback when no `--playback` journal exists. studio/screens/settings.js + new GET /graph endpoint: reads the real house knowledge graph (hub/config/house.json via hub.twin.graph.KnowledgeGraph) for real rooms/assets, lists the AI's actual supported intents, and is explicit that Users has no accounts/auth implemented. 4 new tests (test_graph_endpoint_*), 54 total green. **Verified live**: restarted the running server to pick up the new endpoints, curl confirmed /graph returns the real asset list (main_valve, water_tank, ...) and every new studio file serves. Headless DOM-shim exercise used a /playback.json blob built from an **actual recorded journal** (not hand-written) and proved History's scrubbing is genuinely time-aware — scrubbing to t=0 shows zero incidents, scrubbing to the end-state shows the real leak incident, exactly matching the underlying journal. Insights correctly surfaced the real leak/health-suspect/escalation/NILM cards from captured data; the command bar correctly surfaced a real point (`main_valve.valve_state`) when searching "valve" and the Energy screen when searching "energy" |
| DOMORA Studio — Phase 4 (Room/Appliance/Sensor detail pages) | ✅ Done | studio/screens/{room,appliance,sensor}.js + .css. core/router.js grew `:param` route support (`/room/:id`, `/appliance/:name`, `/sensor/:key`), first-match-wins. Room page covers the 3 real zones (living/utility/panel) with real points, health, and (utility only, honestly — it's the only zone anything ever acts on) the real action list; click-through from Home's zone cards. Appliance page reads GET /nilm and explicitly lists health score/RUL/failure-probability/sound-vibration as **not computed** rather than inventing numbers, since nothing in this codebase measures them; click-through from Energy's NILM bars (bar-chart.js gained an `onItemClick`). Sensor page is fully generic — works for *any* twin point key since every point shares the same {value,t,source,confidence} shape — showing live value/source/confidence/age(vs the same 15-tick staleness convention as hub/twin/state.py), a trend chart, and a raw-values table; click-through from Room's points list. No 3D view/predictions/automation/maintenance-suggestions/calibration UI — none of that is computed anywhere in this codebase, so none of it was built. Headless DOM-shim exercise (same real captured `/ws` frames as Phase 3) drove all three pages including their honest not-found/no-data paths (unknown room id, unknown appliance name, unknown sensor key) — every assertion passed, including the real leak/valve-suspect state on the Utility room, the real kettle 7.50 Wh + honest not-computed rows on the Appliance page, and a real numeric trend vs. a real boolean value with no fabricated chart on the Sensor page. Live-curl confirmed the new files serve and that /nilm honestly returns an empty ledger (not a 404 or fake data) on a scenario that runs no panel power. No Python changes this phase; 52 tests still green |
| DOMORA Studio — Phase 3 (Energy/Water/Security dashboards) | ✅ Done | studio/screens/{energy,water,security}.js + .css. Loaded the `dataviz` skill before writing chart code: ui/line-chart.js (single-series trend, hover crosshair+tooltip, sequential hue) and ui/bar-chart.js (NILM breakdown, categorical, direct-labelled) built to its mark specs; the 3-slot categorical palette + validated against DOMORA's actual light/dark surfaces via `validate_palette.js` (all pass; aqua slot sub-3:1 on light is mitigated by the always-on direct label, matching the skill's relief rule) and committed to tokens.css. New GET /history (journal-backed charts, --playback only, honest 404 otherwise) and GET /nilm (authoritative NILM ledger) endpoints; core/store.js grows a bounded client-side series buffer for the no-journal live case. 12 new tests (test_history_endpoint.py, +4 in test_ai_endpoint.py, +1 threading regression in test_store.py), 52 total green. **Found and fixed two real bugs via verification, not inspection:** (1) `JournalReader`'s sqlite3 connection was thread-affine, crashing `/history`/`/nilm` under `ThreadingHTTPServer` — a latent bug from Phase 2's own journal wiring that nothing had exercised through a real thread until now; fixed with `check_same_thread=False` (read-only, safe) + a regression test. (2) reconstructing the NILM ledger client-side from raw twin points was unsound — `energy.py`'s cluster-relabelling never retires the old point keys (deliberately, per the twin's "silence is a signal, no delete" design), so a relabelled appliance briefly showed up twice under real captured data; fixed architecturally by serving the ledger from GET /nilm (the same `energy.report()` ground truth `ai_query.py` and `test_nilm.py` already trust) instead of re-deriving it. **Verified live**: real running `energy` scenario answers `/nilm` with the true kettle 7.50 Wh / cluster1 0.83 Wh breakdown; real `--playback` server (built from an actual recorded leak run) answers `/history` with real time-ordered flow/level points. Headless DOM-shim exercise fed real captured `/ws` frames (via an actual WebSocket connection to a real server, not hand-written fixtures) through all three screens — Energy's bar chart, Water's leak/incident list + dual trend charts, Security's occupancy + real alert feed all rendered correctly, including the NaN-free chart paths. **Honest limits**: Security screen has no door/motion/gas/smoke data source (no simulator publishes it) and says so explicitly rather than fabricating widgets; Environment dashboard skipped entirely this pass for the same reason (zero real data path — ENV node firmware exists but isn't simulated) and isn't in the nav; session trend charts are this-session-only unless the server runs in --playback mode |
| DOMORA Studio — Phase 2 (DOMORA AI) | ✅ Done | hub/services/ai_query.py — deterministic, offline grounded query engine (no LLM): keyword-routed intents (power/NILM, leak, appliance health, water usage, "what changed while away", status, honest "simulation not wired up yet" for what-if questions) answer strictly from the twin/NILM ledger/SQLite journal, never invented. `/ai` POST endpoint added to hub/services/api.py (energy agent + journal now live on DashboardServer). studio/screens/ai.js — chat screen, quick-prompt chips, evidence cards, voice mic stubbed (disabled, honestly labelled not-implemented). 12 tests (test_ai_query.py) + 2 endpoint tests (test_ai_endpoint.py), 46 total green. **Verified live**: real running `energy`-scenario hub answers "why is power high?" with the true kettle 7.50 Wh / cluster1 0.83 Wh NILM breakdown over the real `/ai` endpoint; real `stuck`-scenario hub correctly answers leak/no-leak on both sides of the escalation. Headless DOM-shim exercise (shimmed `fetch` too) drove a real quick-prompt click through the actual module and confirmed both chat bubbles and the evidence card render correctly. **Honest limits**: no true NLU (keyword routing only); "yesterday"/calendar-day framing doesn't apply yet (system tracks sim ticks, stated plainly in the water-usage answer); simulations, voice, and an optional LLM rephrasing layer remain explicitly un-implemented per docs/APP_PLAN.md |
| DOMORA Studio — Phase 0 (foundation) + Phase 1 (Live Home) | ✅ Done | docs/APP_PLAN.md. `studio/` — self-contained ES-module app, no build step/CDN/framework: tokens.css + base.css (dark-glass design language, 5-state health vocabulary ok/warn/crit/learn/pred), core/{store,socket,router,dom,format,twin}.js, ui/{glass-card,stat-tile,health-dot}.js, screens/home.js. Hub extended (hub/services/api.py) to serve `studio/` at `/studio/` (path-traversal guarded), alongside the existing `/` dashboard, unchanged. 5 new tests (test_studio_serving.py): serving, content-types, 404, traversal guard, dashboard-unaffected — 32 total, all green. **Verified live**: real server, real `stuck` scenario, `curl` confirms `/studio/` and `/` both serve correctly. **Verified by exercising, not inspecting**: a headless Node harness fed the actual studio/core/twin.js + screens/home.js real captured twin snapshots from a live `wire("stuck")` run (t=0 nominal, t=56 leak-suspected+valve-suspect escalation) — caught and fixed two real bugs this way: (1) the harness's own store-singleton race (asserting after both renders let a later store.reset() repaint an earlier target in place — fixed by asserting immediately per render), (2) a genuine app bug in twin.js: an empty house was coded as the *Learning* state instead of a neutral *OK*, which would have tinted an ordinary "nobody home" as if the AI were still calibrating. Both paths now confirmed: nominal → hero/zone render `st-ok`; escalation → both render `st-crit`. **Honest limits**: not opened in a real browser (no browser available in this environment — headless DOM-shim + live curl is the verification that exists); DOMORA AI, dashboards, detail pages, command bar/history/insights remain 📋 Todo per the phased plan |
| Dashboard — twin view (schematic SVG) | ✅ Done | dashboard/index.html — inline-SVG floor plan bound to live twin points: occupancy glow (house.occupied), tank level bar (water_tank.level_pct), animated pipe flow (tank.line.flow_lpm) that turns red on virtual.water.leak_suspected, main-valve state + pulsing suspect badge (health.main_valve), status strip. Twin/Operations tab toggle. **Deviation from spec §10 (Three.js):** built dependency-free SVG instead — no library to vendor, no CDN at the venue, honors "data *is* the graphics". 3D remains a Phase-2 polish option. Verified headless (node DOM shim) against recorded leak (valve closed, nominal) and stuck (valve suspect, flood ongoing) runs, incl. mid-incident scrub at t=35 |

## Hardware

| Component | Status | Notes |
| --- | --- | --- |
| BOM ordering (Tier M subset, ≈₹15k) | ✅ Done | docs/BOM_ORDER.md — ordered 2026-07-23 (§25.6 MVP scope, ≈₹15,490). Awaiting delivery; UNO Q is the lead-time item. Firmware (weeks 3–4) and demo rig unblock as parts arrive |
| Node identity reconciliation | ✅ Done | Found sim/virtual_house.py (fp1=valve+flow), sim/virtual_pump.py (fp2=tank+pump), sim/virtual_loads.py (fp4=panel) didn't match docs/BOM_ORDER.md's actual ordered quantities (1 ESP32-C6 for panel, 1 combined for valve+flow+level+pump+leak — not split). Consolidated the water side onto one identity (`fp2`), freed `fp1` for the panel node, matching tools/gen_certs.py's default node list (which had it right all along). **Assumption, not a confirmed physical layout** — see nodes/README.md; the actual valve/tank wiring plan isn't decided. 23 tests still green (no logic depended on the specific node-id) |
| Node identity map (hub/config/nodes.py) | ✅ Done | The board-to-role assumption above now lives in **one importable place** instead of being hardcoded per-simulator: role-named constants (PANEL/VALVE/TANK/ENV/PERCEPTION), with VALVE_NODE and TANK_NODE both = `fp2` today. Sims read identities via module attribute at publish time (a from-import would freeze the value and make a repoint silently not propagate — stated in the module docstring); tools/gen_certs.py's `--nodes` default derives from `default_pki_nodes()` (order-preserving dedupe: a shared board yields one cert, and the derived list is test-pinned equal to the long-standing `["fp1","fp2","env1"]`). **The split the docs promised is now proven, not promised**: tests/test_node_identity.py makes the exact one-line edit in-memory (`TANK_NODE="fp3"`) and watches pump+level topics move boards, flow/valve stay put, and the PKI default grow — plus runs both flagship closed loops with never-before-seen identities (`fp8`/`fp9`) to prove the hub's cognition keys on assets, never boards. 5 tests, 66 total green. Checked for broker ACL templates that could drift (none exist in-repo — the UNO Q broker ACL was hand-deployed, so **re-provisioning after any split is a bring-up-day step, not a repo file**). Doesn't decide the physical layout — makes the decision cheap whenever the rig answers it |
| TANK node firmware (ESP32-C6, fp2) | 🔄 Ongoing | nodes/tank_node/tank_node.ino — main valve (2-relay timed drive), YF-S201 flow, AJ-SR04M level, SCT-013-030 pump CT (true-RMS), leak probes x2, pump cutoff relay (spare hardware, confirmed with owner). **The hardwired leak reflex**: checked first in every loop, before any MQTT/Wi-Fi, latching, no auto-reopen. Dry-run cutoff deliberately NOT hardwired (needs current+level-slope correlation, that's the hub's job). Found and closed a real gap first: BOM had no pump-cutoff actuator at all (CT clamp only senses) — resolved by owner supplying a spare relay. Found and fixed a fail-safe bug during design: pump relay must default OFF at boot, not ON, or a watchdog reset during an active dry-run cutoff would silently re-energize the pump. Compile-checked in 6 configs (full/bare/4 partial mixes exercising leak-reflex↔valve interaction and the CT/relay split). **Not flashed** — no valve/CT/level/flow hardware in hand yet. **CT measurement bug fixed 2026-08-03** (found while writing the panel node): `read_pump_current_a()` assumed the ADC midpoint was 2048 instead of measuring it, and RMS cannot tell an offset from signal — a ~25-count bias reported 0.251 A with the pump off, above the 0.2 A `pump_state` threshold that `PumpProtection` reads, i.e. a dry-run cut on a pump that was never running. Now the same measured-midpoint single-pass routine as the panel node, with a time-bounded whole-cycle window (40 ms, deliberately shorter than the panel's 100 ms since sampling blocks the leak reflex) and a 64-bit accumulator. `PUMP_ON_THRESHOLD_A` promoted from a magic `0.2f` to a documented config constant |
| ENV node firmware (ESP32-C6) | 🔄 Ongoing | nodes/env_node/env_node.ino — Wi-Fi, mTLS MQTT (birth/LWT), LD2410/PIR/BME280/VEML7700/reed, watchdog. **Compile-checked** against the real ESP32-C6 toolchain (arduino-cli 1.5.0, esp32 core 3.3.9) in 3 sensor-mix configs (full/bare/partial) — a real include-order bug was caught and fixed this way. **Not flashed or run on hardware.** config.h/certs.h gitignored (.example templates checked in); Observer gained temp_c/humidity_pct/pressure_hpa/lux/door range validation, 3 tests. **IR blaster added 2026-08-03** — this node is no longer sensor-only: it subscribes to `domora/cmd/living_ac/+` and transmits captured IR codes, making it the only board that commands something it cannot sense. Deliberately **zero new libraries**: the 38 kHz carrier is the ESP32's own LEDC peripheral and the frame is a raw captured mark/space list, so there's no brand-protocol table to be wrong about and nothing to vendor at the venue (no IR library was installed on this machine, and fetching one was avoidable). Includes the capture/learn path (TSOP receiver → prints a paste-ready timings array over serial), which is also where the safety review's "capture COOL, not HEAT" gap is enforced. Node-side defense in depth: refuses to transmit `on` when its own radar says the room is empty — independent of the hub's identical invariant, same discipline as the tank node's leak reflex. Compile-checked in **6 configs, zero warnings** (`--warnings all`) including blaster-with-radar and blaster-without-radar, since the empty-room guard is itself behind `#if HAS_LD2410`; confirmed genuinely linked in (+11.8 KB flash / +1.2 KB RAM vs the no-IR build), not silently guarded out. **Entirely unverified physically: the carrier, duty cycle and frame timings have never met a scope or a real appliance.** IR LED + TSOP are not in docs/BOM_ORDER.md — pocket change, but parts that must be added |
| FLOW/POWER panel node (fp1, electrical CTs) | 🔄 Ongoing | nodes/panel_node/panel_node.ino — 3x SCT-013-030 CT clamps (true-RMS, DC midpoint **measured** not assumed via single-pass `mean(x²)−mean(x)²`, whole-number-of-cycles sampling window, configurable noise floor) + PZEM-004T v3 over Modbus-RTU (library `PZEM004Tv30` 1.2.1, chosen over hand-rolled Modbus because an untestable CRC/frame parser fails silently as `NAN` on demo day). **Sensing-only: no relay, no reflex, subscribes to no command topic** — spec §11.1 makes panel work sensing-first with mains actuation through certified contactors, and neither §25.6 closed loop touches mains. Publishes the `main_panel/power_w` NILM contract point plus PZEM voltage/frequency/PF/energy and per-clamp `current_a`; every PZEM getter is NAN-guarded (silence is a signal; a fabricated number is not). Compile-checked in **6 configs, zero warnings** (full/bare/CT-only/PZEM-only/mixed/single-clamp) against the real ESP32-C6 toolchain. Found a real bug this way: the .ino preprocessor hoists generated prototypes above sketch-defined types, so a `CtChannel&` parameter was forward-referenced before it existed. **Not flashed** — no CT clamp or PZEM in hand. Two config traps documented loudly because they fail silently: a clamp on the incomer already contains its sub-circuits (double-counting), and a CT is unsigned so it cannot tell import from export (a solar-feed clamp must never be in the aggregate) |
| TANK node (fp2: valve + flow + level + pump + leak reflex) | 🔄 Ongoing | Firmware drafted + compile-checked (see software table). Hardware pending — no valve/CT/level/flow parts in hand. Pump-cutoff relay was a real gap (not in the ordered BOM); owner has a spare 5V relay to wire in |
| Perception node (S3 audio) | 📋 Todo | Stretch — closed set of 6 events |
| Demo rig (2-room model, water loop) | 📋 Todo | Resettable ≤ 60 s |

## Activity Log

| Date | Action |
| --- | --- |
| 2026-07-18 | Spec written (docs/DOMORA_SPEC.md) |
| 2026-07-18 | Repo scaffolded: agent runtime, twin, simulator, tests — both closed-loop scenarios verified green |
| 2026-07-18 | SQLite journaling shipped (store.py + twin hook + tests) — action audit trail captures cause→evidence→expectation→outcome |
| 2026-07-18 | Benchmark harness shipped; dev-PC dry run 245× headroom. WAL sidecar sizing bug found and fixed. Real gate awaits UNO Q hardware — 5 tests green |
| 2026-07-18 | Installed Mosquitto 2.1.2 + paho-mqtt 2.1.0; MQTT bridge shipped and proven end-to-end over a real broker (field node and hub sharing nothing but MQTT) — 6 tests green |
| 2026-07-18 | mTLS shipped: PKI provisioning tool + TLS in the bridge + locked broker config. Proven: certified roundtrip, cert-less rejection, ACL namespace confinement — 9 tests green. Week 2 of PLAN.md complete |
| 2026-07-19 | NILM shipped (week-6 item pulled forward): disaggregates fridge/kettle/lamp from one noisy aggregate power stream, energy ledger within 1% of truth, labelling flow demoed in `--scenario energy` — 13 tests green |
| 2026-07-23 | BOM order list drafted (docs/BOM_ORDER.md): itemized per-node breakdown for the §25.6 MVP subset (not full Tier M), ≈₹15,490 total, UNO Q called out as the order-first/lead-time item |
| 2026-07-23 | Components ordered (§25.6 MVP subset, ≈₹15,490) — awaiting delivery; firmware/rig work unblocks on arrival |
| 2026-07-23 | Dashboard slice 1 shipped (week-5 item pulled forward while parts ship): stdlib-only HTTP+WebSocket server (hub/services/api.py, no FastAPI) taps the live bus+twin; dashboard/index.html renders the cause→evidence→command→verify timeline + live points, theme-aware; per-client queue keeps a slow browser from stalling the hub. Verified live: page serves, 725 events + 1259 points streamed, stuck-valve escalation reached the client. 15 tests green. Three.js house + journal playback remain |
| 2026-07-23 | Dashboard slice 2 shipped: journal playback. store.JournalReader rebuilds a recorded run as the live frame contract; /playback.json serves it; the dashboard grows a scrubber (play/pause + range) that replays the incident through the same renderers. Verified live: stuck run → 1261-frame timeline (t 0–119), dispatched shutoff + escalation reconstructed. 17 tests green. Three.js twin view is the last week-5 item |
| 2026-07-23 | Dashboard slice 3 shipped: schematic SVG twin view (Twin/Operations tabs) bound to live twin points — occupancy glow, tank level, animated leak-aware pipe flow, valve state + suspect badge. Chose dependency-free SVG over spec §10's Three.js (nothing to vendor, no venue CDN). Verified by running the real dashboard JS headless (node DOM shim) against recorded leak/stuck runs + a mid-incident scrub — visual states correct on both paths. **Week 5 (dashboard) complete.** 17 tests green |
| 2026-07-23 | Second closed loop shipped: pump dry-run protection (week-6 item). sim/virtual_pump.py + PumpProtection virtual sensor + planner dry-run rule; cut verified through the CT clamp independent of the relay ACK. Both paths proven (`dryrun` verified at t=33; `dryrun_stuck` retries, escalates, marks pump suspect at t=50) and streamed live over the dashboard. No safety capability-table change needed. **Both §25.6 closed loops now closed.** 20 tests green |
| 2026-07-24 | Pushed 3 pending commits (dashboard, dry-run loop, pump twin view) to origin/main |
| 2026-07-24 | ENV node firmware drafted (week-3 item, pulled forward): Wi-Fi + mTLS MQTT (birth/LWT) + LD2410/PIR/BME280/VEML7700/reed on the same topic contract sim/virtual_house.py already exercises. Discovered a real ESP32-C6 toolchain on this machine (arduino-cli + esp32 core 3.3.9 already installed) and compile-checked the firmware against it in 3 configs — caught and fixed a genuine include-order bug (config.h's HAS_* flags were read before being defined). Observer gained range validation for the new point types (temp_c/humidity_pct/pressure_hpa/lux/door), 3 tests. **Honest limit: never flashed or run on a physical board** — parts arrived same-day. 23 tests green |
| 2026-07-24 | **UNO Q connected — week-1 gate resolved.** Board's Linux side reachable over adb (ADB interface + bundled adb.exe, both auto-detected on this machine). Pushed hub/sim/tools/tests/dashboard over adb, ran the real benchmark ON the board: **30.3x headroom, COMFORTABLE** (3 runs, 29.7–30.4x, consistent). Also ran all 19 stdlib-only tests directly on-device via a tiny dependency-free runner (no pytest installed there) — leak loop, dry-run loop, NILM, journaling, and the live dashboard WebSocket all pass on the real aarch64/Python-3.13.5 target, not just the x86 dev machine. Fixed a benchmark.py reporting bug found along the way: peak_python_heap_mb rounded a genuine ~27 KB peak down to a meaningless "0.0"; renamed to peak_python_heap_kb. Stress test at 2x MVP scale (20 nodes, 8 points, 5 Hz, 120s) still holds 3.6x ("acceptable"), showing the real ceiling is well past the competition scope. **PLAN.md's week-1 exit test is met: no LAN-sidecar fallback needed.** |
| 2026-07-24 | **Dashboard proven as a live service on the real board**, not just a batch test: launched `python3 -m hub.services.api --scenario stuck` on the UNO Q itself, tunneled the port over the USB/adb link (`adb forward`, since the board has no Wi-Fi/LAN configured yet — noted as a stand-in for the eventual Wi-Fi transport, not a difference in the server code), and drove the flagship stuck-valve demo from this laptop. The full cognitive event chain arrived over the wire from the physical board: dispatch at t=34 (`leak:main_line → close main_valve`), escalation at t=56 with the exact expected reason. This is the literal "served from the hub, judge's phone needs no install" claim (dashboard/README.md), verified end-to-end on target hardware |
| 2026-07-24 | Before drafting TANK node firmware, found the ordered BOM has a CT clamp on the pump (sensing only) but no relay/SSR to actually cut its power — closing this gap in software alone wasn't possible, so stopped and asked. Owner confirmed a spare 5V single-channel relay is available. Then drafted nodes/tank_node/tank_node.ino: valve (2-relay timed drive), YF-S201 flow, AJ-SR04M level, CT clamp true-RMS current, leak probes, and the hardwired leak→valve reflex (checked first every loop, before any network code, latching, no auto-reopen — independent of the hub's own safety.py invariant, defense in depth not duplication). Caught and fixed a fail-safe design bug before it shipped: the pump relay must default OFF at boot, not ON — a watchdog reset landing mid-dry-run-cutoff would otherwise silently re-energize a pump the system had just correctly stopped. Compile-checked in 5 configurations against the real ESP32-C6 toolchain. **Not flashed** — no hardware in hand for this node yet. 23 tests still green (no Python touched) |
| 2026-07-24 | Owner prioritized building **DOMORA Studio** — the premium "OS" front-end (product name, not a real OS). Wrote docs/APP_PLAN.md: a self-contained web app (no build step, no CDN, served straight from the hub so the venue/2 GB-UNO-Q "no-install" promise holds), a unique dark-glass design language (health-color + ambient motion = the living-organism feel), every pixel bound to a real twin point or cognition topic (grounded against the actual `/ws` contract, not invented). Phased delivery with per-phase observable definition-of-done: Foundation → Live Home (hero) → DOMORA AI (grounded query engine, offline/deterministic first, optional LLM adapter behind hub/services) → Energy/Water/Security/Environment dashboards → Room/Appliance/Sensor pages → command bar/history/insights. AR/VR/robotics/voice/multi-house explicitly kept in the spec's Phase-2+ backlog. Extends (not replaces) the week-5 `dashboard/`; PLAN.md competition scope untouched. **Plan only — no app code written yet.** |
| 2026-07-25 | **DOMORA Mobile Phase B shipped**: AI chat screen (mobile/lib/screens/ai.dart), ported from Studio's ai.js. Refactored HubClient to accept an injectable http.Client so network code is testable without a live server; tests use byte-accurate real `/ai` response bodies curled from the live hub this session. Caught a real fixture bug along the way — hand-retyping curled JSON swapped the server's actual ASCII-escaped `—` (json.dumps's ensure_ascii default) for a literal em-dash, which broke Latin-1 encoding in the mock response and initially looked like an unrelated widget-test failure. 7 new tests, 13 total green, analyze clean. Empirically confirmed (via curl with a cross-origin Origin header) that the hub sends no CORS headers, so HTTP-based screens can't be live-verified through the Chrome web target the way Home's WebSocket was — MockClient-with-real-data is the correct verification path there; native targets aren't affected |
| 2026-07-25 | **DOMORA Mobile Phase A shipped** (owner chose full Flutter parity with Studio over a thinner companion): new `mobile/` Flutter project — 3 dependencies only, ChangeNotifier for state, no external state-management/chart packages. Direct ports of the already-proven web data layer (store.js→store.dart, socket.js→hub_client.dart, twin.js→twin.dart, including the already-fixed occupancy bug) plus a Connect screen (this app is a separate process from the hub, unlike Studio, so it has to be told the hub's address) and the Home hero screen. `flutter test`'s 6 tests reuse the exact real captured `/ws` frames from Studio's own verification and caught a real overflow bug in the header layout, fixed. Live-verified twice: found the available Android target is missing `getprop` even under raw adb (a sandboxed/minimal instance, not deployable from here), then verified via Chrome web instead — the actual compiled app, real browser, real WebSocket, tracked the real hub's leak escalating live end-to-end. AI/Energy/Water/Security stay honest "not built yet" placeholders pending Phases B–E |
| 2026-07-25 | **DOMORA Studio Phase 0 + Phase 1 shipped**: build-free ES-module app (tokens/base CSS, core store/socket/router/dom/format/twin, ui primitives, Live Home hero screen) served by hub/services/api.py at `/studio/` (path-traversal guarded, `/` dashboard untouched). 5 tests added, 32 total green. Verified live against a real running `stuck`-scenario server (curl) and, more importantly, verified *behaviorally*: fed real captured twin snapshots from an actual `wire("stuck")` run through the real twin.js/home.js and asserted the rendered classes — nominal state green, leak+valve-suspect escalation red. That exercise caught two real bugs before they shipped: a race in the verification harness itself, and a genuine twin.js defect (empty house wrongly coded as the "Learning" state). Both fixed. Not yet opened in an actual browser (none available here) — AI chat, dashboards, detail pages, and command/history/insights remain Todo per docs/APP_PLAN.md's phasing |
| 2026-07-25 | **DOMORA Studio Phase 2 shipped**: hub/services/ai_query.py, a deterministic grounded query engine (power/NILM, leak, appliance health, water usage, "what changed while away", status, honest not-yet-implemented for simulations) — no LLM, every number traced to the real twin/NILM/journal. Wired a `/ai` POST endpoint into hub/services/api.py and built the studio/screens/ai.js chat screen (quick prompts, evidence cards, disabled voice stub). 14 new tests (test_ai_query.py + test_ai_endpoint.py), 46 total green. Verified against real running scenarios over the real endpoint (energy scenario's kettle/cluster breakdown, stuck scenario's leak state on both sides of escalation) and with a headless DOM-shim exercise that drove an actual chip click through fetch and confirmed both bubbles + evidence card render |
| 2026-07-25 | **DOMORA Studio Phase 3 shipped**: Energy/Water/Security dashboards with real charts (loaded `dataviz` skill first; line/bar chart primitives built to its mark specs, categorical palette validated against DOMORA's real surfaces). New /history and /nilm endpoints. Found and fixed two real bugs through verification: a thread-affinity crash in `JournalReader` (latent since Phase 2, only surfaced once something actually queried it from a request thread) and an unsound client-side NILM reconstruction that double-counted a relabelled appliance — replaced with a server-authoritative /nilm endpoint. 12 new tests, 52 total green. Verified live against a real running energy-scenario server and a real --playback server built from an actual recorded run, plus a headless exercise that fed real captured WebSocket frames through all three screens. Security and Environment scoped honestly: no fabricated sensor data where no simulator produces it; Environment skipped entirely pending a real data source |
| 2026-07-25 | **DOMORA Studio Phase 4 shipped**: Room/Appliance/Sensor detail pages, reached by clicking through from Home/Energy/Room rather than top nav. Router grew `:param` support. Room covers the 3 real zones; Appliance reads the real /nilm ledger and is explicit that health-score/RUL/failure-probability/vibration analysis aren't computed anywhere in this codebase (no invented numbers); Sensor is fully generic since every twin point shares one shape. Verified with the same real-captured-frame headless exercise as Phase 3, covering both the data-present and honest-not-found path for all three pages, plus a live curl check that the new files serve and /nilm degrades honestly (empty ledger, not fake data) on a scenario with no panel power. No Python changes; 52 tests still green |
| 2026-07-25 | **DOMORA Studio Phase 5 shipped — all 5 planned phases of the app now complete.** Command bar (⌘K, search-and-navigate only — no manual actuation surface added, since that would be a new safety-relevant capability needing the invariant review CLAUDE.md requires for capability-table changes), AI Insights (prioritized real observation cards), History (genuine time-aware scrubbing of a real recorded journal via GET /playback.json — verified scrubbing to t=0 shows no incidents while the end-state shows the real one), and Settings (new GET /graph endpoint exposing the real house knowledge graph from hub/config/house.json, honest about no user accounts existing). 4 new tests, 54 total green. Verified live against a restarted real server and headless against real captured/recorded data |
| 2026-07-24 | Owner connected the UNO Q to home Wi-Fi (same network as their laptop/phone) — the board now has its own IP and real internet, superseding the earlier USB-tether-only state. Installed Mosquitto 2.0.21 + python3-paho-mqtt via apt (owner supplied the sudo password) as a **separate manual instance on port 8883**, alongside — not replacing — the system's own default plain-MQTT service on port 1883. Provisioned the real house PKI (hub, fp1, fp2, env1, perc1) and deployed the exact mTLS+ACL config already proven in test_mtls.py. First connection attempt from this laptop over the real LAN failed TLS hostname verification — found and fixed the actual bug: tools/gen_certs.py's broker cert only ever had `localhost`/`127.0.0.1` in its SAN, so any genuinely networked client (real ESP32, or anything not on loopback) could never validate it. Added `--broker-host` to put real LAN addresses in the SAN (4 tests, tests/test_gen_certs.py). With the regenerated cert: **the full flagship leak-loop closed end-to-end over the real network** — hub (all 7 agents + twin) running on the board, a simulated field node reaching it from this laptop over actual home Wi-Fi, mTLS + ACL both genuinely enforced. Verified 3 broker guarantees (certified roundtrip, cert-less rejection, ACL confinement) from the laptop against the board's real IP, then the complete closed loop (leak detected t=187 → dispatch → command over the real wire → valve closed t=36 on the field side → verified t=189). Cleaned up afterward: manual broker instance stopped, board tidy, system's default Mosquitto untouched. 27 tests green |
| 2026-07-25 | DOMORA Mobile app icon set: owner supplied `domora_app_icon.png` (1254×1254, already square — no crop needed), moved to `mobile/assets/icon/icon.png`. Wired `flutter_launcher_icons` (dev-dependency only, build-time tool, no runtime footprint added) to generate every Android mipmap density and iOS AppIcon.appiconset size from the one source image instead of hand-cropping each. Verified genuinely applied, not just files-on-disk: rebuilt the debug APK and confirmed the embedded `ic_launcher.png` byte-for-byte matches the freshly generated file (36,525 bytes) |
| 2026-07-30 | **DOMORA Mobile Phases C, D and E shipped — the app now has full Studio parity and every planned software component in this repo is complete.** Energy/Water/Security dashboards with hand-built charts (CustomPainter line chart with a touch crosshair instead of a hover one; widget-composed bars so each is tappable), Room/Appliance/Sensor drill-downs, journal History scrubbing, AI Insights, Settings, and a navigate-only search screen. All fixtures were curled from real running hubs first and replayed as raw bytes — no hand-written JSON. 34 new tests (47 total), analyze clean, Python suite untouched at 54. Verification caught three genuine layout bugs no amount of reading would have found — two `StatTile` overflows (horizontal in the 3-up grid, vertical at the chosen aspect ratios) and a 456px row overflow on map-valued twin points — plus one failure that was the *test harness* being less realistic than the app (`InkWell` needs the `Material` that `AppShell`'s `Scaffold` provides), which was fixed in the harness rather than by bending the app. Live-verified against two real hubs over a real WebSocket and real HTTP, and the compiled app was screenshotted mid-incident showing the real chain `leak:main_line → line flow falls below 0.1 L/min · pending` at t=43, with the valve still green — the same view at t=112 showed it red after the real escalation, so the screenshots capture genuine state evolution rather than a static render. Also replaced `mobile/README.md`'s `flutter create` boilerplate with a real one. Found along the way (not a bug, worth knowing for demos): the hub's WebSocket replay ring buffer is 500 frames, ~43 ticks of a full run, so a client joining late in a scenario legitimately sees the twin's current state but not the earlier cognition events |
| 2026-08-03 | **PANEL node firmware drafted (fp1) — the last unwritten node firmware in the §25.6 MVP.** `nodes/panel_node/`: 3× CT clamps + PZEM-004T v3, publishing the `main_panel/power_w` point NILM has only ever been fed by `sim/virtual_loads.py` until now. Deliberately **sensing-only** — no relay, no reflex, no command subscription — because spec §11.1 makes panel work sensing-first behind certified contactors, and neither MVP closed loop acts on mains; that is a design decision worth stating, not an omission. Compile-checked in 6 sensor-mix configurations against the real ESP32-C6 toolchain, zero warnings, which caught a genuine bug: the `.ino` preprocessor hoists generated prototypes above sketch-defined types, so a `CtChannel&` parameter was forward-referenced before it existed. Used the maintained `PZEM004Tv30` library rather than hand-rolling Modbus-RTU, precisely because a hand-written CRC that cannot be bench-tested fails silently as `NAN` on demo day. **The substantive finding**: a CT clamp measures current only, so with no PZEM on the incomer the firmware must assume a power factor it cannot measure — so rather than assert the consequence in a comment, `sim/virtual_loads.CtChain` models the chain and `tests/test_nilm.py` measures it through the real NILM primitives. Result: appliances stay separable (3 distinct clusters — the demo survives), but a compressor motor's wattage reads **+53.3%** high while a resistive kettle reads 5.0% low, so the energy ledger does not survive. That is now the documented reason `POWER_FROM_PZEM = 1` is the recommended config. Observer gained 4 new ranges + 5 tests, including one pinning the guard's own failure mode (a genuine 258 V surge and 82 V brownout must be *recorded*, not quarantined). 61 tests green (was 54). **Not flashed — no CT clamp, PZEM, or ESP32-C6 was connected to the machine this was written on** (verified: zero COM ports present) |
| 2026-08-03 | **TANK node CT measurement bug fixed** — found by writing the panel node's version of the same routine, not by reading the tank node. `read_pump_current_a()` computed RMS about a hardcoded 2048 instead of the measured DC midpoint, and RMS cannot distinguish an offset from signal, so any bias in the clamp's bias network became phantom current. Checked the arithmetic rather than estimating it: with the pump genuinely off, a 25-count bias reports **0.251 A → `pump_state` "on"**, above the 0.2 A threshold, while the corrected formula reports 0.020 A regardless of bias. At a real 5 A load the two agree within 0.5%, so this was specifically a low-current bug — precisely where the on/off decision `hub/twin/virtual_sensors.py`'s `PumpProtection` consumes actually lives, meaning the failure mode was a dry-run cut on a pump that was never running (or a real dry-run masked by a reading that never changes). Now the same single-pass `mean(x²)−mean(x)²` routine as the panel node, 64-bit accumulator, and a time-bounded whole-cycle window — set to 40 ms rather than the panel's 100 ms on purpose, because sampling blocks and this is the board carrying the hardwired leak reflex, so ADC time is reflex-blind time. `PUMP_ON_THRESHOLD_A` promoted from a magic number to a documented constant next to the calibration it depends on. Recompiled clean in 6 configurations, zero warnings. Still not flashed |
| 2026-08-03 | **fp2 split made cheap** — the one-board-for-the-whole-water-side assumption (highlighted as unresolved in nodes/README.md) can't be *decided* in software, but its cost could be collapsed: new hub/config/nodes.py holds role-named identities (VALVE_NODE/TANK_NODE both `fp2` today), the three simulators and tools/gen_certs.py's default now derive from it, and tests/test_node_identity.py proves the promise instead of restating it — monkeypatching `TANK_NODE="fp3"` moves the pump/level topics to the new board, keeps flow/valve where they are, and grows the PKI default to `["fp1","fp2","fp3","env1"]` with zero edits outside the map; a second test runs both flagship closed loops under identities no config ever mentioned (fp8/fp9) and both still close, proving hub cognition is identity-agnostic by construction, not accident. Deliberate detail: sims read the map via module attribute at publish time, because a from-import would copy the value and make a future repoint silently not propagate — the failure mode this module exists to prevent. Also checked the one place a hardcoded identity could hide outside Python: no broker ACL template exists in-repo (the UNO Q ACL was hand-deployed), so post-split re-provisioning is flagged as a bring-up step in PROGRESS rather than left to be discovered. 66 tests green (was 61) |
| 2026-08-03 | **Third closed loop shipped: comfort — an IR-commanded AC, the first actuator that cannot acknowledge anything.** Owner-requested after a discussion of how DOMORA reaches appliances of other brands that aren't IoT at all; **explicitly outside PLAN.md's 8-week scope and spec §25.6**, recorded here as a scope decision rather than folded in silently. The architectural case for it: every actuator until now was one the hub could also sense (valve position report, pump CT clamp), so verification had a fallback even if the expectation contract were weak. An AC driven by a captured IR code answers *nothing* — there is no ACK even in principle — so the contract carries the entire weight. Verified through a step change at the panel CT, a sensor the appliance cannot influence. `sim/virtual_comfort.py` models a first-order RC room, a compressor with a real spin-up lag (which is *why* the expectation has a deadline rather than checking the next tick), and the `ir_blind` fault — blaster misaimed or LED dead, the exact silent failure an ACK-trusting system cannot see. That failure path is the demo: `living_ac.ir_last_cmd` reads "on", the naive system reports success, and the panel says no compressor ever started → retry → escalate → `health.living_ac` suspect (`test_blind_ac_shadow_state_would_have_fooled_an_ack_trusting_system` asserts exactly that divergence). New `ACRunning` virtual sensor is an edge detector, not a threshold, because the panel reading is aggregate and a fixed line would confirm an AC command a kettle happened to satisfy; it latches on the rising edge so the point holds while the compressor runs, with the honest limit (it still can't tell a compressor from any other ~1.2 kW load starting in the same seconds — NILM's clustering is the real disambiguator) stated rather than designed around. **Capability-table change carries a written invariant review** per CLAUDE.md: `living_ac:on` is the first entry that energizes rather than stops something, bounded by a new invariant (never run the AC in an empty house — which is not a bolt-on guard but the action's own precondition made checkable), and the review names the gap it does *not* close: the table admits `on`/`off` but not a mode, so a reversible heat pump learned from its HEAT button would satisfy every check in the hub. Enforcement therefore lives in which IR code gets captured, and that is documented where the capture happens. Firmware landed on the existing env node rather than a new board — the blaster is an LED on hardware already in the BOM — with **zero new libraries** (LEDC generates the 38 kHz carrier; raw captured mark/space replay means no brand-protocol table and nothing to vendor at the venue), plus the capture/learn path and a node-side radar check that refuses `on` into an empty room independently of the hub. 6 compile configs, zero warnings under `--warnings all`, and the IR code confirmed genuinely linked (+11.8 KB flash) rather than guarded out. 7 new tests, 73 green. **Honest limit: the IR path has never met a scope or a real appliance** — compiling is not evidence an AC will react, and the first bench step must be capture-replay-does-it-beep before the loop is trusted |
| 2026-07-25 | Docs site shipped: `tools/build_docs_html.py` renders `docs/*.md` (spec, app plan, BOM, agent instructions) into an interactive HTML site at `docs/html/`, reusing Studio's actual design tokens (`studio/tokens.css`) rather than inventing a new look. Real markdown-it-py rendering (tables, heading anchors, TOC), the spec's 8 Mermaid architecture diagrams rendered live via a vendored `mermaid.min.js` (no CDN), copy-able code blocks, scroll-spy navigation, and a client-side search index built from every heading section. Caught and fixed a real bug during verification: the build script's own cleanup step deleted the hand-authored `assets/style.css`/`app.js` and never restored them, so the first render was completely unstyled — moved those to `tools/docs_html_templates/assets/` as the real source and had the build copy them in fresh each run. Verified by serving the output over a local HTTP server and screenshotting it with headless Edge (`file://` URLs turned out to silently block the stylesheet — a real gotcha worth remembering), confirming the landing page, TOC/search, code blocks, and a live-rendered Mermaid diagram all render correctly before committing. Repo history was also reconciled here: this entry, plus the Studio app, `ai_query.py`, and the Mobile app above, had accumulated across many sessions without ever being committed — all pushed to origin/main together |
| 2026-08-04 | **DOMORA Mobile: the Connect-screen gate removed — owner reported it locked you out of the app entirely with no address in hand.** `HubClient.baseUrl` made mutable; `connect()` now guards an empty address into a new `unconfigured` state (`DomoraStore.connection`) instead of falsely claiming `connecting`, and a new `reconnectTo(url)` cancels any in-flight attempt, resets the store, and redials — the operation Settings' new **Hub connection** card calls (address field, a non-blocking reachability hint via the existing `healthCheck()`, the `10.0.2.2:8080` emulator shortcut carried over). `main.dart` builds `AppShell` directly now; a saved address loads and dials in the background if one exists. `connect.dart` deleted outright. AppShell's status pill gained the new state and is now tappable; a dismiss-free banner across every tab points at the same Settings card whenever the hub isn't live — the discoverability the old gate gave away for free, kept without the lockout. 9 new tests (56 total green), `flutter analyze` clean. **Verified live on a real device**: this session's connected Android target (unlike Phase A's, which was sandboxed/undeployable) turned out fully functional — `adb`/`getprop`/`screencap` all worked — so the debug APK was actually built, installed, and driven end-to-end over real home Wi-Fi against a real running hub, with screenshots at each step: app data cleared to force a genuine first-ever launch → opens straight to Home, no gate, honest "not set up" pill; banner tap reaches Settings; typing the real LAN address and saving flips the pill green and streams the real leak-suspected/critical escalation live; force-stopping and relaunching reconnects automatically with no re-prompt. (Incidentally found port 8080 already bound by an unrelated process on this machine via `netstat`; used 8081 rather than investigating further) |
